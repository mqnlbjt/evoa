{
	"id": "smoke",
	"name": "Smoke Suite",
	"description": "A minimal deterministic suite for checking that the CLI, runtime, model client, and grader are wired together.",
	"tasks": [
		{
			"id": "smoke-task",
			"type": "general",
			"title": "Smoke exact answer",
			"prompt": "Reply with exactly: pong",
			"scoring": {
				"method": "exact",
				"maxScore": 1,
				"config": {
					"expected": "pong",
					"trim": true,
					"caseSensitive": true
				}
			},
			"metadata": {
				"example": true
			}
		}
	],
	"metadata": {
		"example": true
	}
}
